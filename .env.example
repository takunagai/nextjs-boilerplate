# このファイルは環境変数の設定例です。以下の手順で使用してください：
# 1. このファイルを `.env.local` にコピーしてください: `cp .env.example .env.local`
# 2. 必要な値を実際の値に置き換えてください
# 3. `.env.local` はGitにコミットされないため、安全に機密情報を保存できます
#
# 注意: 
# - `.env.local` は開発環境用です
# - 本番環境では、デプロイプラットフォーム（Vercel, Netlify, Cloudflare等）の環境変数設定を使用してください
# - テスト環境では `.env.test` を使用できます
# - Cloudflare Workers では wrangler.toml の vars セクションでも環境変数を設定できます

# アプリケーション設定
NEXT_PUBLIC_APP_URL=http://localhost:3000 # アプリURL
NEXT_PUBLIC_APP_NAME="Next.js Boilerplate" # アプリ名
NEXT_PUBLIC_SITE_NAME="Next.js Boilerplate" # サイト名
NEXT_PUBLIC_SHOW_EXAMPLES=true # サンプルページを表示/非表示 (default: true)
NODE_ENV=development # 環境

# データベース接続情報
# PostgreSQL / Neon / Supabase など
DATABASE_URL="postgresql://postgres:password@localhost:5432/mydatabase"

# 認証関連
# 32文字以上のランダムな文字列を生成してください: `openssl rand -base64 32`
AUTH_SECRET=your-auth-secret-key-min-32-chars
NEXTAUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true # Cloudflare Workers では true に設定
# Note: AUTH_SECRETを使用（NextAuth v5推奨）。NEXTAUTH_SECRETは不要

# (オプション) OAuth プロバイダー設定
# GITHUB_ID=
# GITHUB_SECRET=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# API キー
# 外部サービスのAPIキー
# NEXT_PUBLIC_ プレフィックスは、クライアントサイドで使用する環境変数に付けます
# NEXT_PUBLIC_API_KEY=
# PRIVATE_API_KEY=

# Cloudflare 固有の設定
# Cloudflare アカウント設定（デプロイ時に必要）
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Three.js パフォーマンス最適化設定
NEXT_PUBLIC_3D_QUALITY=medium # high/medium/low
NEXT_PUBLIC_PRELOAD_3D=false # 3D リソースの事前読み込み

# Cloudflare Images（オプション）
# CLOUDFLARE_IMAGES_ACCOUNT_HASH=your-images-hash
# CLOUDFLARE_IMAGES_API_TOKEN=your-images-api-token